import React, { Fragment } from 'react';
import spinner from './6.gif';

const Spinner = () => {
    return (
        <Fragment>
            <img src={spinner}></img>
        </Fragment>
    )
}

export default Spinner
