import React, { Fragment } from 'react';
import spinner from '../images/6.gif';

const Spinner = () => {
    return (
        <Fragment>
            <img src={spinner} alt=""></img>
        </Fragment>
    )
}

export default Spinner
